<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout
    tools:context=".SelectEquipmentDialogFragment"
    android:layout_centerInParent="true"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <ImageView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/close_btn"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:focusable="true"
        android:clickable="true"
        android:layout_gravity="top|start"
        android:layout_marginEnd="4dp"
        android:contentDescription="@string/label_close_btn"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/equipment_type_list"
        android:name="com.remixtrainer.SelectEquipmentItemFragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_below="@id/close_btn"
        android:layout_toEndOf="@id/close_btn"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:context=".SelectEquipmentItemFragment"
        tools:listitem="@layout/fragment_select_equipment_item" />
</RelativeLayout>